export default function Header() {
  return (
    <div>
      <h1 className="appTitle">Tenzies</h1>
      <p className="appInstruction">
        Roll until all dice are the same.
        <br />
        Click each die to freeze it at its current value between rolls.
      </p>
    </div>
  );
}
